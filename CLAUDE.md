# PDFラベルメーカー

## プロジェクト概要
テキストを入力し、ラベル用紙に合わせたPDFを生成するWebアプリケーション。

## 技術仕様
- HTML + JavaScript(サーバー不要、ブラウザだけで動く)
- PDF生成にはjsPDFライブラリを使う(CDNから読み込む)
- エントリポイント: `index.html`(ブラウザで直接開いて動作する)
- jsPDFはバージョンを固定して読み込む
  - `https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js`
  - v2系のUMD版なので `const { jsPDF } = window.jspdf;` で取り出す
- 単位はすべてmm(jsPDFも `new jsPDF({ unit: 'mm', format: 'a4' })` で初期化する)

## 日本語テキストの扱い
jsPDFの標準フォント(Helvetica等)は日本語を描画できず、文字化けまたは空白になる。
そのため、テキストは**Canvasに描画してから画像としてPDFに貼り付ける**方式で出力する。

- `document.createElement('canvas')` にテキストを描画し、`toDataURL('image/png')` で画像化
- `doc.addImage()` でラベル位置に配置する
- Canvasは高解像度で描く(実サイズ × 4倍程度)。そのまま等倍で描くと印刷時にぼやける
- この方式ならフォントファイルの埋め込みが不要で、1ファイル完結の規約を保てる

## ラベルの仕様
- 用紙サイズ: A4(210mm × 297mm)
- 余白(上下左右)を設定できる
- ラベルのサイズ(幅×高さ)を設定できる
- ラベル間の余白を設定できる
- テキストを入力し、すべてのラベルに同じ内容を印字する

### 初期値
起動直後にそのまま出力できるよう、以下をデフォルト値とする(A4で3列×8行=24面になる)。

| 項目 | 初期値 |
| --- | --- |
| 上下左右の余白 | 10mm |
| ラベル幅 | 60mm |
| ラベル高さ | 30mm |
| ラベル間の余白(横・縦) | 5mm |
| フォントサイズ | 12pt |

### 面数の計算
行数・列数はユーザーに入力させず、用紙サイズ・余白・ラベルサイズから自動計算する。

- 列数 = floor((210 - 左余白 - 右余白 + 横のラベル間余白) / (ラベル幅 + 横のラベル間余白))
- 行数 = floor((297 - 上余白 - 下余白 + 縦のラベル間余白) / (ラベル高さ + 縦のラベル間余白))

計算結果が0以下になる設定(ラベルが用紙に収まらない)の場合は、PDFを生成せず画面にエラーを表示する。

### 表示と出力の挙動
- テキストはラベル内で上下左右中央に配置する
- 入力欄はtextareaとし、改行はラベル内でも改行として反映する
- ラベル幅に収まらない行は自動で折り返す。折り返しても高さに収まらない分は切り捨てる
- ラベルの外枠は細い罫線で描画する(印刷位置の確認用)
- 生成したPDFは `doc.save()` でダウンロードさせる
- 現在の設定で「何列×何行 = 何面」になるかを、PDF生成前に画面に表示する

## コーディング規約
- コメントは日本語で書く
- 1つのHTMLファイルにまとめる(外部CSSや外部JSファイルは作らない)
- CSSは `<style>`、JavaScriptは `<script>` としてHTML内に直接書く

## 動作確認
- サーバーは不要。`index.html` をブラウザで直接開いて確認する
- 確認する観点: 初期値のままPDFが生成できるか / 日本語が文字化けしないか / 面数の表示が実際の出力と一致するか / ラベルが用紙に収まらない設定でエラーが出るか

## 作業ルール
- 変更前に必ずgit commitすること(作業単位ごとに区切ってコミットする)
- コミットメッセージは `種別: 内容` の形式。種別は `add:` / `update:` / `fix:` を使い、内容は日本語で書く
